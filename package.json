{
  "name": "novaenglish-next-api",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Next.js API Routes backend for Nova English (monorepo with React + Vite FE).",
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "next build",
    "start": "bash start_safe.sh",
    "deploy": "npm run prisma:generate && npm run prisma:deploy && npm run build",
    "postinstall": "npm run deploy",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio",
    "prisma:seed": "node prisma/seed.js"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "bcryptjs": "^3.0.2",
    "jsonwebtoken": "^9.0.2",
    "next": "^15.0.0",
    "nodemailer": "^6.9.14",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "zod": "^3.23.8",
    "prisma": "^5.18.0"
  },
  "devDependencies": {
    "pm2": "^5.4.3"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "keywords": [
    "nextjs",
    "api",
    "auth",
    "prisma",
    "postgresql"
  ],
  "license": "MIT"
}
